from typing import List


def three_sum(nums: List[int]) -> List[List[int]]:
    res = []
    for i in range(len(nums)):
        one = nums[i]
        for two in nums[i + 1:]:
            three = 0 - one - two
            if three in nums[]

    ...
